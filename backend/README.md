# Shorten Server

Built with Flask.

## Getting Started

To get started add an environment variable in the `backend/` directory as such.

```bash
env FLASK_APP=run.py
```

## Starting the Server

```bash
flask run
```